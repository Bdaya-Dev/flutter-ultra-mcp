// ultra_flutter_devtools — Flutter DevTools extension UI.
//
// Wave-2 owner: devtools worker (see plan §12).
// Loaded by Flutter DevTools as an iframe extension; pairs with the
// flutter-ultra-devtools MCP server's WebSocket protocol per plan §6.2.

library ultra_flutter_devtools;
