// ultra_flutter_sentry_compat — Sentry binding compat layer.
//
// Wave-1 owner: worker-B (task #2). See plan §6.1 Path B and the
// 09-marionette-headless-driving rule for the historical singleton-race
// context that motivates this mixin.

library ultra_flutter_sentry_compat;
