# Changesets

This directory stores [changesets](https://github.com/changesets/changesets) — short markdown files describing changes that should be released.

Run `npm run changeset` (or `npx changeset`) to author a new changeset. The release workflow consumes these to bump versions and generate `CHANGELOG.md` entries per package.
