// @flutter-ultra/mcp-runtime — common MCP server scaffolding.
//
// Wave-2 dependency for every server package under packages/.
// Owns: stdio transport wiring, lifecycle (initialize, shutdown), tool registration helpers,
// the watchdog/cancellation wrapper from plan §17, structured stderr logging.

export const PACKAGE_NAME = '@flutter-ultra/mcp-runtime';
