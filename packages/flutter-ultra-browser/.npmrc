# Skip Playwright's auto-download of browser binaries.
# Browsers download via `npx playwright install chromium` in CI when needed,
# placed under ${CLAUDE_PLUGIN_DATA}/browsers per plan §4 .mcp.json env.
playwright_skip_browser_download=1
