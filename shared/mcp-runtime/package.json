{
  "name": "@flutter-ultra/mcp-runtime",
  "version": "0.0.0",
  "private": true,
  "description": "Shared MCP server scaffolding: lifecycle, transport, tool registration, watchdog/timeout, structured logging.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -b",
    "lint": "eslint src",
    "test": "vitest run",
    "typecheck": "tsc -b --noEmit",
    "clean": "rimraf dist .turbo *.tsbuildinfo"
  }
}
