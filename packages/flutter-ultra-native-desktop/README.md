# @flutter-ultra/flutter-ultra-native-desktop

MCP server for **native desktop UIs** that Flutter cannot reach via VM Service: Windows UIA (NodeRT or FlaUI sidecar), macOS AXUIElement (Swift sidecar), Linux AT-SPI (PyGObject sidecar).

**Status:** scaffold stub. Implementation owner: **wave-2 native-desktop worker** (see plan §12).
