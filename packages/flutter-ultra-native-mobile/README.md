# @flutter-ultra/flutter-ultra-native-mobile

MCP server for **native mobile overlays** that Flutter cannot reach via VM Service: Android UIAutomator via `adb`, iOS XCUITest via `xcrun simctl` + `go-ios`.

**Status:** scaffold stub. Implementation owner: **wave-2 native-mobile worker** (see plan §12).
