{
  "name": "@flutter-ultra/flutter-ultra-runtime",
  "version": "0.0.0",
  "private": true,
  "description": "MCP server for Flutter runtime: discover + attach to flutter run sessions over DDS, introspect VM Service.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "flutter-ultra-runtime": "dist/index.js"
  },
  "files": ["dist", "README.md"],
  "scripts": {
    "build": "tsc -b",
    "lint": "eslint src",
    "test": "vitest run",
    "typecheck": "tsc -b --noEmit",
    "clean": "rimraf dist .turbo *.tsbuildinfo"
  }
}
