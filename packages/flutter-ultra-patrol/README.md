# @flutter-ultra/flutter-ultra-patrol

MCP server orchestrating **Patrol E2E tests** (web + Android + iOS). Wraps `patrol_cli` and bundles the Bdaya patrol fork at `vendor/patrol/`.

**Status:** scaffold stub. Implementation owner: **wave-2 patrol worker** (see plan §12).

Vendoring the patrol fork is the responsibility of wave-2 **worker O** (per plan §15 revised rev 21).
