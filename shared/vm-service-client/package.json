{
  "name": "@flutter-ultra/vm-service-client",
  "version": "0.0.0",
  "private": true,
  "description": "TypeScript Dart VM Service client with DDS multi-client coordination. Wave-1 worker C delivers the implementation.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -b",
    "lint": "eslint src",
    "test": "vitest run",
    "typecheck": "tsc -b --noEmit",
    "clean": "rimraf dist .turbo *.tsbuildinfo"
  }
}
